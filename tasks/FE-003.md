# Отчёт по задаче FE-003

## Статус

Выполнено.

## Что сделано

- Проведена верификация DoD задачи FE-003 по существующей реализации из FE-001:
  - ✅ Список типов сортируется по имени (backend: `ORDER BY name ASC`).
  - ✅ Поиск фильтрует список по названию (case-insensitive).
  - ✅ Empty state при пустой базе содержит CTA «Добавить первый тип», модалка не открывается автоматически.
- Исправлен UX-дефект: состояние «поиск не дал результатов» теперь использует отдельный i18n-ключ `pages.main.emptySearch`:
  - `frontend/src/shared/i18n/resources/ru.ts` — добавлен ключ `emptySearch: "Ничего не найдено."`;
  - `frontend/src/shared/i18n/resources/en.ts` — добавлен ключ `emptySearch: "No results found."`;
  - `frontend/src/pages/MainPage.tsx` — блок empty search использует `t("pages.main.emptySearch")` вместо `t("pages.main.empty")`.

## Верификация

- `tsc --noEmit` — успешно (0 ошибок).
- `eslint .` — успешно (0 ошибок).
- `vite build` — успешно (dist/ собран).
- Backend тесты — 25 passed.

## Принятые решения

- ADR: [`ADR-0023`](../ADR/ADR-0023-main-screen-empty-search-state-fe-003.md)
