# FE-004: Создание типа (модалка) + обработка дубликатов

## Статус: Выполнено

## Что сделано

### Новые файлы
- `frontend/src/components/CreateTypeModal.tsx` — модальный компонент создания типа на базе нативного `<dialog>`.

### Изменённые файлы
- `frontend/src/pages/MainPage.tsx` — кнопки «Добавить тип» и CTA empty state теперь открывают модалку; после создания список перезагружается.
- `frontend/src/app/router.tsx` — удалён маршрут `/types/new`.
- `frontend/src/styles.css` — добавлены стили для модального окна.

### Удалённые файлы
- `frontend/src/pages/CreateTypePage.tsx` — заменена модалкой.

## Проверка DoD
- [x] Тип создаётся через модальное окно.
- [x] Список обновляется сразу после создания (re-fetch).
- [x] Дубликаты обрабатываются UX-адекватно: `ERR_DUPLICATE_TYPE_NAME` → локализованное сообщение в модалке.
- [x] Frontend quality gate: `tsc --noEmit` (0 ошибок), `eslint .` (0 ошибок), `vite build` (собран).
- [x] Backend тесты: 25 passed.

## Принятые решения
- [ADR-0024](../ADR/ADR-0024-create-type-modal-fe-004.md) — модалка вместо отдельной страницы.
