# Отчёт по задаче API-007

## Статус

Выполнено.

## Что сделано

- Реализован endpoint `GET /api/v1/export` в `backend/app/api/v1/router.py`:
  - добавлена выгрузка всего состояния по всем типам;
  - для каждого типа экспортируются `name`, `stage_counts` и `history`;
  - экспорт отсортирован детерминированно (типы: `name ASC`, история: `created_at ASC, id ASC`).
- Расширены API-схемы в `backend/app/api/v1/schemas.py`:
  - добавлены `ExportStageCount`, `ExportHistoryItem`, `ExportTypeItem`, `ExportResponse`.
- Добавлены интеграционные тесты `backend/tests/test_export_api.py`:
  - пустая база возвращает `{"types": []}`;
  - заполненная база возвращает все типы, counts по всем стадиям и полную историю.
- В `BACKLOG.md` задача `API-007` отмечена выполненной.
- В `CHANGELOG.md` добавлена запись о задаче.

## Верификация

- `make quality` — успешно (backend format/lint/tests, frontend format/lint/typecheck/build/test).

## Принятые решения

- ADR: [`ADR-0018`](../ADR/ADR-0018-export-endpoint-api-007.md)
