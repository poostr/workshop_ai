# Отчёт по задаче DB-002

## Статус

Выполнено.

## Что сделано

- Подтверждён и зафиксирован bootstrap Alembic:
  - конфиг `backend/alembic.ini`;
  - окружение миграций `backend/alembic/env.py`;
  - стартовая ревизия `backend/alembic/versions/0001_init_schema_placeholder.py`;
  - автопрогон при старте контейнера `backend/entrypoint.sh` (`alembic upgrade head`).
- Добавлен интеграционный тест `backend/tests/test_migrations.py`, который:
  - применяет `alembic upgrade head` к временной БД;
  - проверяет создание ключевых таблиц и таблицы версии миграций.
- В `BACKLOG.md` задача `DB-002` помечена выполненной.
- В `CHANGELOG.md` добавлена запись по `DB-002`.

## Верификация

- `make backend-test` — успешно.
- `make frontend-test` — успешно.

## Принятые решения

- ADR: [`ADR-0009`](../ADR/ADR-0009-alembic-bootstrap-db-002.md)
